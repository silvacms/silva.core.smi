<div class="middleground">
  <div class="transporter">
    <tal:button tal:repeat="button viewletmanager.buttons"
                tal:replace="structure button.render()" />
  </div>
  <div class="executor" tal:condition="viewletmanager.executors">
    <tal:button tal:repeat="button viewletmanager.executors"
                tal:replace="structure button.render()" />
  </div>
</div>
